.tab_title {
  display: flex;
}
.tab_title .title_item {
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 15rpx 0;
  flex: 1;
}
.tab_title .active {
  color: var(--themeColor);
  border-bottom: 5rpx solid currentColor;
}
